# Destroy the clusters and Multipass VMs
ansible-playbook multipass_destroy.yml

# Print a message indicating that the clusters and VMs have been destroyed
echo "The clusters and VMs have been destroyed."